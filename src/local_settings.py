from django.utils.translation import ugettext as _
#ADMINS = (
    #('amon-ra', 'amon.raj@gmail.com'),
#)

#EMAIL_HOST_USER = '
#EMAIL_USE_TLS = True
#EMAIL_HOST = '172.2.6.7'
#EMAIL_HOST_PASSWORD = 'xxxxxx2'
#EMAIL_PORT = 25

#Activate on debugging
DEBUG = True
TEMPLATE_DEBUG = DEBUG
INTERNAL_IPS = ('10.61.0.1',)


#parameters from production db
#DATABASES = {
    #"default": {
        ## Ends with "postgresql_psycopg2", "mysql", "sqlite3" or "oracle".
        #"ENGINE": "django.db.backends.sqlite3",
        ## DB name or path to database file if using sqlite3.
        #"NAME": "dev.db",
        ## Not used with sqlite3.
        #"USER": "",
        ## Not used with sqlite3.
        #"PASSWORD": "",
        ## Set to empty string for localhost. Not used with sqlite3.
        #"HOST": "",
        ## Set to empty string for default. Not used with sqlite3.
        #"PORT": "",
    #}
#}


#LANGUAGES = [
    #('es', 'Spanish'),
#]

#CMS_TEMPLATES = (
    #('page.html',_('Base Page')), 
    #('template_index.html', _('Index page template')),
 
#)

